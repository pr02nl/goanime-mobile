name: paulo_flix_lint_plugin
description: Regras de lint customizadas para o projeto PauloFlix.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.9.0"

dependencies:
  analyzer: ^7.3.0
  custom_lint_builder: 0.7.5
  meta: ^1.17.0

dev_dependencies:
  custom_lint: 0.7.5
